{% set name = "scikit-datasets" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/scikit-datasets-{{ version }}.tar.gz
  sha256: 316a754d455fc39dd9c548612d99e98459cab7aaa255f787d00ade747ebf088a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - pytest-runner
    - python >=3.8
  run:
    - numpy
    - python >=3.8
    - scikit-learn
    - scipy

test:
  imports:
    - skdatasets
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/daviddiazvico/scikit-datasets
  summary: Scikit-learn-compatible datasets
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - vnmabus
